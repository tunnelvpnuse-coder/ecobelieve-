[tool.poetry]
name = "omni-life-ai"
version = "0.1.0"
description = "Omni Life AI service (FastAPI)"
authors = ["Omni Life"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "0.111.0"
uvicorn = {extras = ["standard"], version = "^0.30.0"}
anthropic = "^0.40.0"
python-multipart = "^0.0.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
